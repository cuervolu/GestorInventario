package com.cuervolu.gestioninventario.entities;

/**
 * Enumeración que representa los roles de usuario en el sistema.
 *
 * <p>Los roles definen los niveles de permisos y acceso que un usuario puede tener en el sistema.
 * Los valores posibles son ADMIN (administrador) y USER (usuario).</p>
 *
 * @author Cuervolu
 * @since 1.0.0
 */
public enum UserRole {
  ADMIN,
  USER
}
