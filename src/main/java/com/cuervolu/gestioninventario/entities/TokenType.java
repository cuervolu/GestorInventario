package com.cuervolu.gestioninventario.entities;

/**
 * Enumeración que representa los tipos de token en el sistema.
 *
 * <p>Actualmente, el único tipo de token soportado es BEARER, que se utiliza en el esquema de
 * autenticación Bearer.
 *
 * @author Cuervolu
 * @since 1.0.0
 */
public enum TokenType {
  BEARER
}
